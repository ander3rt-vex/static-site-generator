// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://ander3rt-vex.github.io',
  base: '/static-site-generator',
  output: 'static',
});
